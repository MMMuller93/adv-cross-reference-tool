{
  "name": "nport-api",
  "version": "0.1.0",
  "private": true,
  "description": "N-PORT private-company holdings HTTP API (standalone subsystem).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test --test-reporter=spec tests/nport-routes.test.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "express": "^4.21.0",
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
